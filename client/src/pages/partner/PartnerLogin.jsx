import { useState } from 'react';
import { Handshake, LockKeyhole, Mail } from 'lucide-react';
import { Navigate, useNavigate } from 'react-router-dom';
import { api, getUser, setSession } from '../../lib/api';

export default function PartnerLogin(){const nav=useNavigate(),[form,setForm]=useState({email:'',password:'',remember:true}),[error,setError]=useState(''),[loading,setLoading]=useState(false);if(getUser()?.role==='partner')return <Navigate to="/partner" replace/>;const submit=async e=>{e.preventDefault();setLoading(true);setError('');try{const {data}=await api.post('/auth/partner-login',form);setSession(data.token,data.user,form.remember);nav('/partner',{replace:true});}catch(err){setError(err.response?.data?.message||'Unable to sign in.');}finally{setLoading(false);}};return <main className="partner-login"><section><div className="partner-login-brand"><span><Handshake size={25}/></span><div><strong>Insight Partner</strong><small>SECURE STAKEHOLDER PORTAL</small></div></div><div className="partner-login-copy"><p className="eyebrow">PARTNER ACCESS</p><h1>Your companies, investments and responsibilities.</h1><p>Review authorized company activity, track your investment position and complete tasks assigned by Group Administration.</p></div></section><form onSubmit={submit}><div className="partner-login-icon"><LockKeyhole size={22}/></div><h2>Partner sign in</h2><p>Use the login issued for your Key People profile.</p><label>Email address<div><Mail size={16}/><input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/></div></label><label>Password<div><LockKeyhole size={16}/><input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/></div></label><label className="partner-remember"><input type="checkbox" checked={form.remember} onChange={e=>setForm({...form,remember:e.target.checked})}/>Keep me signed in</label>{error&&<div className="partner-error">{error}</div>}<button disabled={loading}>{loading?'Signing in…':'Sign in securely'}</button></form></main>}
