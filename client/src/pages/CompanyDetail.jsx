import { useEffect,useState } from 'react';import { useParams } from 'react-router-dom';import { api } from '../lib/api';
export default function CompanyDetail(){const {id}=useParams();const [d,setD]=useState(null);useEffect(()=>{api.get(`/companies/${id}`).then(r=>setD(r.data))},[id]);if(!d)return <div className="page">Loading...</div>;return <div className="page"><header className="page-header"><div><p className="eyebrow">COMPANY WORKSPACE</p><h1>{d.company.name}</h1><p>{d.company.industry} · {d.company.company_type}</p></div><span className="status">Active</span></header><div className="dashboard-grid"><section className="panel span2"><div className="panel-head"><h2>Company profile</h2></div><div className="detail-grid">{[['Legal name',d.company.legal_name||d.company.name],['Parent','Sanleo Capital'],['Country',d.company.country],['Currency','INR (₹)'],['Registration No.',d.company.registration_no||'—'],['Website',d.company.website||'—']].map(([a,b])=><div key={a}><span>{a}</span><b>{b}</b></div>)}</div></section><section className="panel"><div className="panel-head"><h2>Ownership</h2></div>{d.shareholders.map(s=><div className="share-row" key={s.id}><div><strong>{s.shareholder_name}</strong><span>{s.shareholder_type}</span></div><b>{s.share_percent}%</b></div>)}</section><section className="panel span2"><div className="panel-head"><h2>Products</h2></div>{d.products.length?<div className="company-list">{d.products.map(p=><div className="company-row" key={p.id}><div className="company-icon">{p.name[0]}</div><div className="grow"><strong>{p.name}</strong><span>{p.description}</span></div><span className="status">{p.status}</span></div>)}</div>:<div className="empty">No products registered</div>}</section></div></div>}
